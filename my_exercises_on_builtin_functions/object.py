# does not accept any arguments!

# cannot add new properties or methods to this object

# This object is the base for all classes, it holds the built-in properties and methods which are default for all classes.

x = object()
print(dir(x))
print(type(x))